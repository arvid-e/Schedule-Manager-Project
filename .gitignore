# Node.js dependencies
node_modules/
.pnpm-store/ # If using pnpm with a global store

# Build artifacts (TypeScript compilation output)
dist/
build/
*.js            # Compiled JavaScript files (if not in a dedicated output dir like 'dist/')
*.js.map        # Source maps generated by TypeScript
*.d.ts          # Type declaration files (if not in a dedicated output dir and you don't want them tracked)

# Environment variables
.env            # Primary environment file (contains secrets, should NEVER be committed)
.env.*

!.env.example   # Keep .env.example if you want to provide a template for others

# Logs
logs/
*.log
npm-debug.log* # Legacy npm debug logs
yarn-debug.log* # Yarn debug logs
.pnpm-debug.log* # pnpm debug logs

# IDE and OS specific files
.idea/          # IntelliJ IDEA projects
.vscode/        # VS Code specific settings (can be useful to share some configs, but not user-specific ones)
*.swp           # Vim swap files
*.swo           # Vim swap files
*.bak           # Backup files
*.tmp           # Temporary files
.DS_Store       # macOS specific files
Thumbs.db       # Windows specific files
.tool-versions  # asdf-vm (version manager)

# NPM related files
npm-shrinkwrap.json # If you use shrinkwrap (less common than package-lock.json)
.npm/

# Test/Coverage reports
coverage/
.nyc_output/    # IstanbulJS coverage output
*.lcov          # LCOV coverage files

# Runtime data
*.pid           # Process ID files
*.seed
*.pid.lock

# Miscellaneous
.DS_Store
.nyc_output
.eslintcache    # ESLint cache file
.prettiercache  # Prettier cache file